%table
  %thead
    %tr
      - for prop in props
        %th= prop[:name]
  %tbody
    - for obj in list
      = view(model_name, :mode => :table_row, :model => obj)
%a{:href => route_to(model_name, 'create')} New