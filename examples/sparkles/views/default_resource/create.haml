%div#edit-box
  %form#edit{:action => "#{packet.route_to(model_name, 'new')}", :method => 'post'}
    - for prop in props
      %p= view_attribute(prop, model_name, :label => true)
    %div#button-container
      %input#publish{:type => "submit", :name => "login-submit", :value => "Save New"}